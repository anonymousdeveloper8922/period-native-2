-- CREATE TABLE DATES (date NOT NULL, symptoms NOT NULL, notes NOT NULL);

-- INSERT INTO dates (date, symptoms, notes)
-- VALUES ("test2", "cramps", "hello2");

SELECT * FROM dates;
-- DELETE FROM dates WHERE date = "test";